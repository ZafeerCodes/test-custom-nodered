// let nodeId = this.id;
// let activeWorkspace = RED.workspaces.active();
// console.log("Active workspace:", activeWorkspace);

// var wires = RED.nodes.node(node.id).wires;

// console.log(wires, "NodeInfo")

// let currNode = RED.nodes.node(nodeId);
// console.log(currNode, "currentNode")

// var allFlows = RED.nodes.nodes;
// const flows = RED.nodes.getAllFlowNodes(nodeId);

// const fullFlowData = RED.nodes.createCompleteNodeSet();

// function getConnectedNodes(nodeId) {
//   return fullFlowData.filter(node => {
//     return node?.wires?.some(wireIds => wireIds.includes(nodeId));
//   });
// }
